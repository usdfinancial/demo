// This page has been removed - Real Auth AA functionality is no longer needed
// Users should use the main login/signup flow instead

import { redirect } from 'next/navigation'

export default function RealOnboardingPage() {
  redirect('/')
}